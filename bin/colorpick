#!/bin/sh
# Shamelessly stolen from Drew DeVault
# https://git.sr.ht/~sircmpwn/dotfiles/tree/master/item/bin/colorpick

color=$(grim -g $(slurp -p) - | convert - txt:- | awk 'END {print $3}')
wl-copy $color
echo $color

