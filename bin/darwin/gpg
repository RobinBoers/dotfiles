#!/bin/bash
# Needed on MacOS, because MacOS, unlike my Linux system
# doesn't run .profile for login shells, so the environment
# variables won't be set where you expect them to be.

source ~/etc/env
/opt/homebrew/bin/gpg $@
