base: /volume1/www/
host: nov.dupunk.to
builder: dec.dupunk.to

registry:
- reg.dupunkto.org
- ghcr.io

aliases:
  reload: docker exec router caddy reload -c /etc/caddy/Caddyfile
  psql: docker exec -it master-db psql -U postgres
  ps: docker ps -a --format 'table ({{.ID}})  {{.Names}}\t\t{{.Image}}\t{{.RunningFor}}\t\t{{.Status}}'
